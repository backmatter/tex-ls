---
source: tests/bib_rule_docs.rs
expression: render_rule_doc(rule.as_ref())
---
## `deprecated-suppression-syntax`

Flag the retired `@comment{meaning-ignore <rule>}` and `@comment{meaning-ignore-file [<rule>]}` suppression spellings, which remain accepted for compatibility but are no longer documented. The Safe autofix rewrites only the family and verb to `meaning-lint skip` or `meaning-lint skip-file`; the selector, reason, delimiters, and remaining entry text stay byte-for-byte unchanged. This meta diagnostic is not silenced by the retired directive it reports; use `[lint].ignore` to disable the rule deliberately.

This rule is **enabled by default**.

A retired suppression directive in a structured comment:

```bib
@comment{meaning-ignore unused-string: intentional}
@string{x = {X}}
```

```text
warning: deprecated-suppression-syntax
 --> references.bib:1:10
  |
1 | @comment{meaning-ignore unused-string: intentional}
  |          ^^^^^^^^^^^^^^ retired suppression syntax; use `@comment{meaning-lint skip …}` instead
```

After applying the fix:

```bib
@comment{meaning-lint skip unused-string: intentional}
@string{x = {X}}
```
